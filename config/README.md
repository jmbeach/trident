This directory requires a file called `config.json` which looks like the
following:

```
{
    "youtubeApiKey": "<your-youtube-api-key>"
}
```

You can get a YouTube API key [here](https://console.developers.google.com/apis/dashboard).
